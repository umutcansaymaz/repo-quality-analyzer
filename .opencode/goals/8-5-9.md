# GOAL
8.5 → 9 için gerekenler: Go + Java gerçek repo golden'ları (~1 saat) + taint'in en azından basit hali (değişken türetme takibi) — derinlik boyutunu %58'den %70'e taşır.

## STATUS
active

## DONE_WHEN
- Go + Java gerçek repo'ları tests/fixtures/repos/ altına klonlandı (.git'siz, küçük)
- golden-real.test.ts'e Go + Java golden'ları eklendi (skor aralığı + secret 0 + injection 0 — kilitli)
- Motor: basit taint — değişkene yapılan SON atama tamamen literal ise sabit sayılır
  (const cmd = "ls -la"; exec(cmd) → FP yok); dinamik türetme riskli kalır
- Audit: injection-var-static (beklenti []) + injection-var-dynamic (beklenti [command_injection])
- Doğrulama: audit 0/0/0, tüm testler, build, lint, mevcut golden'lar bozulmadı

## CONSTRAINTS
- Yalnızca kalite repo'su değişir (motor, audit, tests, fixtures)
- Gerçek repo'lar GitHub'dan klonlanır — küçük ve yaygın projeler seçilir; .git klasörü kaldırılır
- Taint kapsamı basit tutulur: yalnızca son atama kontrolü (tam taint grafiği değil)
- Mevcut davranışlar bozulmamalı: audit 0/0/0 korunmalı

## PLAN
1. Go repo klonla (küçük gerçek proje) → fixtures/repos/go-sample
2. Java repo klonla (küçük gerçek proje) → fixtures/repos/java-sample
3. Motor analizi → değerleri ölç → golden-real.test.ts'e Go/Java testleri ekle (skipIf yol yoksa)
4. Taint: isStaticCommandArg'e değişken türetme kontrolü (identifier → son atama literal mi?)
5. Audit varyantları: injection-var-static / injection-var-dynamic
6. Tam doğrulama + commit + push

## NEXT
- [ ] 1) Go + Java repo klonlama (fixtures/repos)
- [ ] 2) Golden testleri (Go + Java)
- [ ] 3) Taint: değişken türetme takibi
- [ ] 4) Audit varyantları (var-static/var-dynamic)
- [ ] 5) Doğrulama + commit

## PROGRESS
(başlangıç)

## RUNS
- started: 2026-08-04T18:57:15.209Z
